# Dependencies for re-running Symmetry_Organised_Complexity_QNN.ipynb end-to-end.
# Versions pinned to those used to produce the precomputed tables in Results/
# (recorded in Results/run_manifest.json). No GPU, no PyTorch, no quantum SDK required.

numpy>=1.26,<3
scipy>=1.13,<2
pandas>=2.0,<3
matplotlib>=3.7,<4
jupyter>=1.0
